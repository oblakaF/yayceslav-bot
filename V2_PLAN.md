# Яйцеслав V2 — план и фактический статус

База: `feature/human-yayceslav` — **V1, не изменяется**.

Рабочая ветка: `feature/human-yayceslav-v2`.

Текущий статус: **основной V2-каркас реализован и покрыт автотестами; production/Railway остаётся на V1 до отдельного решения владельца.**

Последний полный интеграционный прогон после подключения `runet_classic`: **419 passed**.

## Жёсткие правила V2

- [x] V1 не изменяется: все эксперименты и новые механики идут только в V2.
- [x] Один ответ — один речевой стиль. Никакого смешивания youth/skoof/old-russian/blat/operative/battle/post-irony/исторических пакетов в одной реплике.
- [x] Серьёзные и чувствительные темы подавляют агрессивные/мемные стили.
- [x] Полезность ответа выше персонажа: сначала факт/решение, затем характер.
- [x] Новые механики вводились этапами и проверялись полным `pytest`.

## 1. Единый селектор речевого стиля — ✅ КОД + ТЕСТЫ

- [x] Введён единый `voice_pack` на ответ (`style_engine.py`).
- [x] Пакеты: `classic`, `youth`, `skoof`, `old_russian`, `blat`, `operative`, `battle_2017`, `post_irony`.
- [x] Исторические независимые пакеты: `runet_2007`, `runet_2012_2016`, `lan_2000s`, `runet_classic`.
- [x] `operative` — скрытый редкий стиль, не отображается в пользовательских настройках.
- [x] `serious` принудительно выбирает `classic`.
- [x] `voice_runtime.py` физически берёт лексику только из одного выбранного пакета.
- [x] Старый V1-смешиватель словарей не используется в V2-пути.

## 2. Динамическая длина и антизанудство — ✅ КОД + ТЕСТЫ

- [x] Классы: `micro` 45–170, `short` 120–320, `normal` 260–600, `long` 520–950 символов-ориентиров.
- [x] Выбор зависит от контекста и рандомизируется.
- [x] `short/normal/detailed` пользователя — bias, а не жёсткий размер.
- [x] Хранится история длины по чату.
- [x] После длинного ответа повышается вероятность короткого (`verbosity fatigue`).
- [x] Два соседних ответа одного чата **не получают один и тот же класс длины**, если есть допустимая альтернатива.
- [x] Даже внутри классов `target_chars` меняется случайно.
- [x] Простым вопросам разрешён ответ одной-двумя фразами без обязательного объяснения/итога.
- [x] V2-base prompt запрещает лекционный тон, повтор вывода и обязательные вводные/заключения.

Контроль: этап длины после усиления ритма — **417/417 tests PASS**, commit `9763dd7`.

## 3. Подключение уже существовавших, но спящих механизмов — ✅ / ОСОЗНАННЫЕ ИСКЛЮЧЕНИЯ

- [x] `RUNET_2007` подключён отдельным `runet_2007`.
- [x] `RUNET_2012_2016` подключён отдельным `runet_2012_2016`.
- [x] `LAN_GAMING_2000S` подключён отдельным `lan_2000s`.
- [x] `RUSSIAN_INTERNET_CLASSICS` подключён отдельным `runet_classic`, а не смешан с `runet_2007`.
- [x] `OLD_RUSSIAN_METAPHORS` используется внутри отдельного `old_russian`.
- [x] Для ранее объявленных типов `irony`, `hyperbole`, `gaming_terminology`, `deadpan_official`, `anti_joke`, `comic_refusal` добавлены реальные поведенческие подсказки.
- [x] `callback_reference` реально передаётся в V2-поведение.
- [x] Старый `HumorDecision.selected_address` **намеренно не активируется**: адрес выбирает `voice_runtime` из текущего пакета, иначе можно нарушить правило одного стиля.
- [x] `last_humor_strategy` **намеренно не используется как обязательная персистентная механика**: повтор контролируется существующим `RepetitionTracker`, а краткосрочное продолжение поведения — `state_engine`. Не писать в БД ради галочки.
- [ ] `jokes_detected` / `serious_messages` пока остаются резервными полями схемы: не подключать без конкретной пользовательской функции/статистики.

Контроль исторических пакетов: **419/419 PASS**, `runet_classic` commit `384d293`.

## 4. Персонализация поведения — ✅ КОД + ТЕСТЫ

- [x] `relationship_level` передаётся в HumorContext.
- [x] Чем лучше знаком участник, тем немного выше допустимая фамильярность.
- [x] Текущий титул используется только как редкий callback, а не постоянное обращение.
- [x] `joke_archetype`, заданный админом, используется редко и явно трактуется как шуточный ярлык, а не факт о личности.
- [x] Нет постоянной вендетты: конфликтная память краткосрочная и затухает.

Контроль: social/aggression integration — **361/361 PASS**, commit `b151747`.

## 5. Мат и агрессивность / dokop — ✅ КОД + ТЕСТЫ

- [x] `roughness` отвечает за лексику/мат.
- [x] `aggression_engine.py` отдельно отвечает за желание спорить/цепляться.
- [x] Убрано искусственное правило `high = только одно ругательство`; допускается несколько естественных матерных элементов на несерьёзной теме.
- [x] Мат не вставляется ради количества.
- [x] `dokop` может цепляться к слишком категоричному тезису, провокации, возможной смене позиции или слабому месту.
- [x] Для возможного противоречия используется недавний контекст, без выдумывания противоречия.
- [x] Cooldown на конкретного `(chat_id, user_id)` не даёт непрерывно прессовать одного участника.
- [x] Проактивный dokop отключён для серьёзных тем, emotional support, technical/factual help и `roughness=low`.
- [x] Активный dokop переводит последующие минуты в состояние `argumentative`.

## 6. Блатной пакет — ✅ КОД + ТЕСТЫ

- [x] Отдельный `blat`, никогда не смешивается с другими стилями.
- [x] Есть slang, addresses, greetings, taunts, rough, flex, comebacks, wisdoms, grumbling, praise, comparisons, drops.
- [x] Используется комедийная блатная лексика: `базар`, `рамсы`, `кипиш`, `расклад`, `предъява`, `понты`, `малява`, `фраер`, `кент` и т.п.
- [x] Нет реальных инструкций по преступлениям.

## 7. Скрытый оперативный пакет — ✅ КОД + ТЕСТЫ

- [x] Отдельный редкий `operative`, скрытый от `/settings`.
- [x] Казённо-зловещая пародия: «зафиксировано», «материал подшит», «информация ушла куда следует», «это штраф» и т.п.
- [x] Инструкция прямо запрещает утверждать реальную связь с КГБ/ФСБ, реальную слежку или передачу данных.
- [x] Не реализованы шутки с сексуализированным/реальным насилием.

## 8. Battle 2017 — ✅ КОД + ТЕСТЫ

- [x] Отдельный `battle_2017`, не смешивается с youth/LAN/другими стилями.
- [x] Короткие микро-референсы: `изи-изи`, `рил ток`, `синк эбаут ит`, мотив `17:03`.
- [x] Основной материал — оригинальные панчи Яйцеслава.
- [x] Runtime отдельно запрещает длинные чужие баттл-цитаты.

## 9. Постирония — ✅ КОД + ТЕСТЫ

- [x] Отдельный `post_irony`, без смешивания.
- [x] Есть fake expertise, over/underreaction, deadpan, meta-комментарии, сухие псевдовыводы.
- [x] Не требуется объяснять «это шутка» или ставить обязательный emoji.
- [x] Сделан самостоятельный стиль интернет-постиронии, а не копия конкретного автора.

## 10. Умные emoji — ✅ КОД + ТЕСТЫ

- [x] `reaction_engine.py` управляет только Telegram emoji reactions.
- [x] Частота emoji уменьшается на 20% **после** context floor.
- [x] `normal`: 0.70 → 0.56.
- [x] Контекстный floor: 0.85 → 0.68.
- [x] `chaos`: 0.90 → 0.72.
- [x] Текстовый `random_reply_chance` не уменьшен; `normal` остаётся 0.16.
- [x] Есть смысловые причины: provocation, praise, complaint, agreement, mysticism, absurdity, dead_argument, suspicious_drama.

Контроль: **378/378 PASS**, commit `ef642e4`.

## 11. Random drops — ✅ КОД + ТЕСТЫ

- [x] Не создан второй спам-канал.
- [x] Random drop может только заменить содержимое уже разрешённого hard-mode random-reply slot.
- [x] Требуется накопленная активность.
- [x] Есть случайность и cooldown 8 минут.
- [x] Один drop = один voice pack.
- [x] Используется anti-repeat.
- [x] Серьёзные сообщения не разгоняют счётчик random drop.

## 12. Fatigue — ✅ КОД + ТЕСТЫ

- [x] Окно вызовов: 10 минут.
- [x] Порог: 8 обращений к боту.
- [x] Cooldown: 12 минут.
- [x] Ворчание берётся только из `grumbling` текущего voice pack.
- [x] Если текущий pack не имеет grumbling, стиль не переключается ради fatigue.
- [x] На серьёзных темах отключено.
- [x] Бот ворчит, но всё равно отвечает на вопрос.
- [x] Срабатывание переводит последующие реплики в `annoyed`.

Контроль passive engine: **392/392 PASS**, commit `2feeffa`.

## 13. Daily title — ✅ КОД + ТЕСТЫ

- [x] Автотитул выдаётся при первой подходящей активности после 18:00 МСК.
- [x] Кандидаты — только участники, реально писавшие сегодня.
- [x] Число сообщений даёт право участвовать, но не увеличивает вес выбора.
- [x] Предыдущий титул по возможности не повторяется.
- [x] Таблица `daily_title_assignments` хранит результат в SQLite.
- [x] `PRIMARY KEY(chat_id, date)` физически не позволяет два автотитула в одном чате за день.
- [x] Рестарт Railway не должен повторно раздать титул в тот же день.
- [x] Новый день допускает новый титул.
- [x] Назначение обновляет существующий `current_title` участника.
- [x] Если daily title сработал на сообщении, hard-mode не наслаивает на него ещё emoji/random reply.

Контроль: commit `b369fd2`; интеграционный Validate PASS.

## 14. State machine — ✅ КОД + ТЕСТЫ

- [x] Состояния: `cold_start`, `warming_up`, `normal`, `hostile_response`, `serious`, `annoyed`, `argumentative`.
- [x] State — не voice pack и не может смешивать стили.
- [x] `serious` имеет наивысший safety-приоритет.
- [x] `hostile_response` имеет приоритет над annoyed/argumentative.
- [x] После долгой паузы включается `cold_start`, затем `warming_up`, затем `normal`.
- [x] `annoyed` делает ответы короче и суше.
- [x] `argumentative` повышает склонность продолжать спор и удерживать линию аргумента.
- [x] State влияет на length weights и aggression probability, но не выбирает второй стиль.

Контроль: **415/415 PASS**, commit `d6cf3d1`.

## Отложено / сознательно не делаем сейчас

- [ ] Реакция «меня проигнорировали» — отложена: в групповом чате молчание после реплики бота часто нормально и может сделать его навязчивым.
- [ ] `unhinged` не делаем дефолтом.
- [x] Смешивание стилей запрещено архитектурно.
- [ ] `jokes_detected` / `serious_messages` не активируем без конкретного назначения.

## Контрольные коммиты V2

- `b151747` — social context + aggression.
- `f5d5ffc` — изолированные исторические meme packs.
- `ef642e4` — smart emoji reactions.
- `2feeffa` — passive random drops + fatigue.
- `b369fd2` — persistent daily title.
- `d6cf3d1` — character state machine.
- `9763dd7` — жёстко меняющийся ритм длины ответа.
- `384d293` — отдельный `runet_classic` из ранее спящего `RUSSIAN_INTERNET_CLASSICS`.

## Что ещё ОБЯЗАТЕЛЬНО проверить живьём перед любым production-переключением

- [ ] V2 запускается в отдельном тестовом Railway/service или другом безопасном тестовом окружении.
- [ ] В одном ответе визуально не смешиваются два речевых пакета.
- [ ] Длина реально гуляет: очень короткие, средние и длинные ответы чередуются естественно.
- [ ] Короткий вопрос иногда получает короткий ответ без «лекции после ответа».
- [ ] `blat`, `operative`, `battle_2017`, `post_irony`, исторические пакеты реально появляются с ожидаемой редкостью.
- [ ] `operative` не создаёт впечатление реальной слежки/передачи данных.
- [ ] Мат стал естественно жёстче, но не присутствует механически в каждом сообщении.
- [ ] Dokop возникает только там, где уместен, и не преследует одного человека подряд.
- [ ] Emoji визуально стали реже, при этом текстовые hard-mode вмешательства не исчезли.
- [ ] Random drops не увеличили общую частоту вмешательств, а только разнообразили существующие слоты.
- [ ] Fatigue появляется после серии обращений и затем уходит на cooldown.
- [ ] Daily title после 18:00 МСК выдаётся не более одного раза в день на чат и переживает рестарт.
- [ ] После рестарта V2 SQLite-миграции не уничтожают V1-данные.
- [ ] `/geminiversion` подтверждает Gemini 3.6 Flash на реально запущенном V2.

**До выполнения живой проверки Railway остаётся на `feature/human-yayceslav` (V1). V2 не мерджить в V1 и не переключать production без отдельной команды владельца.**
